# Demo 0.1.0 设计与交互验证

final result: passed

日期：2026-09-16。结论针对对齐用前端，不代表正式 DSH 产品验收。没有剩余 P0/P1/P2；下列 P3 差异留待用户反馈。

## 比较依据

- Source visual truth：`../designs/direction-3.png`。
- Implementation：`http://127.0.0.1:4173/`，`../qa/home-final.png`。
- Viewport：1487×1058 CSS px，devicePixelRatio 1；原稿及实现均为 1487×1058 像素，无密度缩放。
- State：首页、默认示例账号、默认桌面对象、右侧最近任务、浅色主题。
- 同一画面比较：`../qa/comparison-final.jpg`（左原稿，右实现，2974×1058）。已查看组合画面。
- 局部证据：`../qa/focus-1.jpg`（标题和桌面）；`../qa/focus-2.jpg`（底部输入）。底部局部对比已查看，文字及控件可读。

## 比较与修复历史

1. 首次组合 `../qa/comparison-initial.jpg`：P2 文件夹图标仅轮廓，桌面主对象视觉分量不足；改用 Phosphor 实心金色文件夹。最终组合确认对象更易辨识。
2. 1280×800 检查发现 P2：主区域撑高 Grid，底部账号不在可视窗口；为 `.app-frame` 设置 `min-height: 0`，使内容区独立滚动。复测主区域底部为 800 px，账号底部为 778 px；证据 `../qa/laptop.png`。
3. 任务成果打开后右侧切回预置任务，存在 P2 状态不一致；改为持续显示当前任务。最终完成态和刷新后任务状态可继续查看。
4. 重置后搜索框仍保留、工作空间未恢复，修复重置 UI 状态；最终首页截图为恢复后状态。

## 五项视觉检查

- 字体：系统中文无衬线（macOS PingFang SC），标题 36 px、导航 18 px、正文 14–17 px，字重与层级接近原稿。文字无乱码，长对象名称省略。P3：原图标题笔画、logo 大小与系统字体存在细微差异。
- 间距和布局：侧栏 238 px、详情 390 px、56 px 顶栏，保留三列桌面、固定底部输入和右侧任务。主要区域与原稿约有 8–20 px 偏移，未改变页面结构或内容密度。1280×800 可滚动；820×900 详情收为可关闭面板，入口仍可访问。
- 颜色：暖白 `#faf9f6`、浅灰侧栏、草绿选中态和 `#315f4f` 主按钮，语义颜色清楚；未出现高饱和背景或重阴影偏离。
- 图标：使用 Phosphor / Fluent Color 标准图标，清晰无拉伸，不使用手绘 SVG 或 emoji。标准文件、清单、知识图标与原稿纹理有意不同；文件类型仍可区分。原稿生成 logo 未作为品牌资产使用。
- 文案：以产品方案为准。示例日期调整、知识对象显示待审阅数量、浏览器去掉假的系统窗口按钮，并补充对齐说明和重置入口；模拟执行及 A/B 样例已标明。

## 交互与日志

已用内置浏览器实际验证任务发起、授权、成果预览、停止/拒绝/重试；桌面新建、列表及搜索；能力配置；Harness A/B 应用回退；知识导入、审阅、关系追溯与来源撤销；协作草稿；账号切换、刷新后持久化、重置；产品 Markdown 说明联动。具体证据摘要见 `../../iterations/002-demo.md`。

检查浏览器 error/warn 日志：无记录。`npm run build` 通过。截图为真实浏览器渲染；主交付视口 1487×1058，另检验 1280×800、820×900。手机尺寸不是本次桌面产品验收范围。

未逐项端到端测试拖拽、导出文件、添加能力和添加账号；它们不作为已验证能力声称。没有真实模型、外部通信、安全隔离或安装包测试。

## 收尾清单

- [x] 同尺寸全图与局部对比
- [x] P2 问题修复并复测
- [x] 主要交互和空账号状态验证
- [x] 产品方案、对应表、启动说明、迭代记录同步
- [x] 保持本地演示，正式工程等待确认
