# 项目协作约定

本目录是基于 DeepSeek Harness 的独立产品仓库。开始任务先读 [README](README.md)、[产品定义](product/brief.md)和[当前待办](iterations/backlog.md)。

- 正式应用编程须先获得用户对对应方案和 Demo 的明确确认，更新[确认记录](product/approval.md)后才能开始，包括正式脚手架、依赖配置和工程实验。可见行为、数据归属、权限及交付范围改变时先更新方案并确认。
- 用户已授权制作交互 Demo 用于对齐理解；Demo 在独立的 demo/ 中实现，范围以[Demo 对应表](product/demo-map.md)为准。它不等于正式工程获准开工，模拟行为须明确标识。
- 产品方案定义完整需求，Demo 对齐样式及主要交互，工程同时依据两者；变更通过稳定功能编号关联，不能只改 Demo 而丢失方案规则。
- 方案审阅期间允许研究、编辑设计文档、整理任务和版本备份，不得把“开始搭建”解释为跳过方案确认。
- 遵循[版本与恢复约定](iterations/versioning.md)，每个验收阶段保留检查点，代码、Harness 配置和用户数据分别恢复。
- v0.1 完成前仅在本地维护 Git、文档和备份，不创建 GitHub 仓库、配置远端或推送。v0.1 之后由用户决定上传，未收到明确指令不自动发布。
- 产品方案写入 `product/`；迭代目标与验收记录写入 `iterations/`；实现写入 `engineering/`。
- 优先使用上游已有能力，通过配置、插件和应用入口定制 Harness。需要修改上游内核时，先记录具体缺口与维护成本。
- 未验证的产品假设明确标为待验证；不得把目录占位、模拟响应或插件加载当作产品闭环完成。
- 每轮迭代确定一个可观察的交付结果，记录实际执行的验证和剩余问题。
- 工程接入时固定依赖版本与锁文件，更新 [上游记录](engineering/upstream.md)，提供可复制的启动和验证命令。
- 只在本仓库执行项目 Git 操作。不要提交凭证、真实会话或用户数据；环境变量通过无密钥的 `.env.example` 说明。
